{
  "name": "suppuo-e2e",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:staging": "FRONTEND_URL=${FRONTEND_URL:-http://staging-suppuo} BACKEND_URL=${BACKEND_URL:-http://staging-suppuo/api/v1} playwright test --config=playwright.ci.config.ts",
    "lint": "tsc --noEmit",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
