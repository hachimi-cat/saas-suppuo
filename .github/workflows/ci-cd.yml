name: CI/CD

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      update_baselines:
        description: 'Regenerate visual regression baselines on this branch and commit them back. Use after an intentional marketing chrome change.'
        type: boolean
        default: false

# Serialize per branch so stacked commits don't fire multiple parallel deploys
# against the staging/prod servers. cancel-in-progress=false because we want
# every push to still reach prod — just in order, not in parallel.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Workflow shell sanity
        # A comment between a \-continued env block and its command silently
        # unsets the env: the command runs bare and nothing errors. That shipped
        # here on 2026-07-14 and took an entire E2E suite down. Cheap to enforce.
        run: python3 scripts/check-workflow-continuations.py
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json
      - name: Backend type check
        run: cd backend && npm ci && npm run type-check
      - name: Frontend lint
        run: cd frontend && npm ci && npm run lint

  test:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    # Independent of lint — they only share `checkout`. Running in parallel
    # cuts ~45s off the pipeline.
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
          POSTGRES_DB: suppuo_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports: ['6379:6379']
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - name: Backend tests
        run: |
          cd backend && npm ci
          DATABASE_URL=postgresql://postgres:test@localhost:5432/suppuo_test \
            npx prisma migrate deploy
          DATABASE_URL=postgresql://postgres:test@localhost:5432/suppuo_test \
            REDIS_URL=redis://localhost:6379 \
            JWT_SECRET=test-secret JWT_REFRESH_SECRET=test-refresh-secret \
            npm test
      - name: Frontend tests
        run: cd frontend && npm ci && npm test

  build:
    name: Build
    runs-on: ubuntu-latest
    # The single source of truth for build artifacts. Backend dist is
    # env-independent and shipped once. NEXT_PUBLIC_API_URL is left as the
    # default `/api/v1` (relative) so the same build works on both
    # staging-suppuo and suppuo.forjio.com — nginx proxies /api/v1 to the
    # backend on both hosts. Server-side env vars (HUUDIS_*, MIDTRANS_*,
    # etc.) are read at runtime from droplet .env and don't require a
    # rebuild when they change.
    #
    # Turnstile-specific: the frontend is built TWICE and two artifacts are
    # uploaded. The STAGING build OMITS NEXT_PUBLIC_TURNSTILE_SITE_KEY
    # (staging is tailnet-locked → captcha is pure friction for E2E +
    # authed screenshots), the PRODUCTION build inlines it. deploy-staging
    # ships frontend-out-staging; deploy-production ships
    # frontend-out-production. Backend enforcement mirrors this — the secret
    # is withheld on staging (see deploy-staging sed) and injected on prod.
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json
      - name: Build backend
        run: cd backend && npm ci && npx prisma generate && npm run build
      - name: Upload backend dist
        uses: actions/upload-artifact@v6
        with:
          name: backend-dist
          path: |
            backend/dist/
            backend/package.json
            backend/package-lock.json
            backend/prisma/
      - name: Install frontend deps
        run: cd frontend && npm ci
      - name: Build frontend (staging)
        env:
          NEXT_PUBLIC_API_URL: /api/v1
          NEXT_PUBLIC_MIDTRANS_CLIENT_KEY: ${{ secrets.MIDTRANS_CLIENT_KEY }}
          # Turnstile is DISABLED on staging — staging is tailnet-locked (no
          # public bots), so the captcha widget is pure friction for E2E +
          # authed screenshots. NEXT_PUBLIC_TURNSTILE_SITE_KEY is omitted =>
          # @forjio/auth-ui hides the widget. The production build below
          # keeps the site key. (Backend enforcement is also withheld on
          # staging — see the deploy-staging sed.)
        run: cd frontend && npm run build
      - name: Upload frontend build (staging)
        uses: actions/upload-artifact@v6
        with:
          # Suppuo's frontend is a DYNAMIC Next app (BFF session portal,
          # public ticket pages with dynamic params) — ship `.next/` and
          # run `next start` on the droplet (pawpado/ripllo model), not
          # the template's static-export `serve out/`.
          name: frontend-out-staging
          # .next is a dotdir — upload-artifact skips hidden files by
          # default, which silently ships an artifact with NO build.
          include-hidden-files: true
          path: |
            frontend/.next/
            frontend/public/
            frontend/package.json
            frontend/package-lock.json
            frontend/next.config.*
      - name: Clean frontend .next for prod rebuild
        run: rm -rf frontend/.next
      - name: Build frontend (production)
        env:
          NEXT_PUBLIC_API_URL: /api/v1
          NEXT_PUBLIC_MIDTRANS_CLIENT_KEY: ${{ secrets.MIDTRANS_CLIENT_KEY }}
          # Cloudflare Turnstile — inlined at build so @forjio/auth-ui renders
          # the widget on every (auth)/ + (admin)/ form. PROD-ONLY.
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: ${{ secrets.TURNSTILE_SITE_KEY }}
        run: cd frontend && npm run build
      - name: Upload frontend build (production)
        uses: actions/upload-artifact@v6
        with:
          name: frontend-out-production
          include-hidden-files: true
          path: |
            frontend/.next/
            frontend/public/
            frontend/package.json
            frontend/package-lock.json
            frontend/next.config.*

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && github.repository != 'hachimi-cat/forjio-service-template'
    steps:
      # Staging lives on the SHARED box reached only over the tailnet
      # (taila71a7b.ts.net). Join as an ephemeral CI node so rsync/SSH below
      # can reach 100.66.19.22. Ephemeral key → the node auto-removes when
      # the job ends.
      - name: Connect to tailnet (ephemeral CI node)
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TS_AUTHKEY }}
          hostname: suppuo-deploy-ci
      - uses: actions/download-artifact@v7
        with:
          name: backend-dist
          path: backend/
      - uses: actions/download-artifact@v7
        with:
          name: frontend-out-staging
          path: frontend/
      - name: Set up deploy SSH key
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
      # De-containerized rsync/ssh. The burnett01/appleboy actions run inside
      # Docker WITHOUT the runner host's tailnet route, so they time out
      # reaching 100.66.19.22 (the box's tailnet IP — private, only routable
      # over the tailnet joined above). Plain rsync/ssh run on the host, so
      # they route over tailscale0. node_modules/.next/cache excluded — the
      # box installs runtime deps via `npm ci --omit=dev` per the script below.
      - name: Deploy via rsync (over tailnet)
        run: |
          rsync -avz --delete \
            --exclude node_modules --exclude .git --exclude .env --exclude .npmrc --exclude frontend/.next/cache \
            -e "ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=30" \
            ./ root@100.66.19.22:/opt/saas/suppuo/
      # bash -se: -e aborts (and fails the job, blocking prod) if
      # `prisma migrate deploy` fails — replacing appleboy's silent
      # script-continue. -s reads the script from the heredoc on stdin.
      - name: Migrate + restart (over tailnet)
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=30 root@100.66.19.22 'bash -se' <<'SCRIPT'
          cd /opt/saas/suppuo/backend
          # Staging is uniformly NON-VERIFYING — strip any captcha secret so e2e never blocks.
          sed -i '/^TURNSTILE_SECRET_KEY=/d; /^TURNSTILE_ALLOWED_HOSTNAMES=/d' .env 2>/dev/null || true
          # --omit=dev — runtime deps only. dist/ + prisma client are
          # already in the artifact from the Build job, so we just need
          # the production node_modules tree here.
          npm ci --omit=dev
          npx prisma migrate deploy
          pm2 restart suppuo || pm2 start dist/index.js --name suppuo -i 1
          cd /opt/saas/suppuo/frontend
          # Dynamic Next app — `.next/` is pre-built in the artifact
          # (no on-box `next build`; 1GB-droplet OOM gotcha). Install
          # runtime deps + run `next start` under pm2.
          npm ci --omit=dev
          pm2 restart suppuo-frontend || pm2 start npm --name suppuo-frontend -- start
          pm2 save
          # Wait for backend to be healthy
          echo "Waiting for backend health..."
          for i in $(seq 1 30); do
            if curl -sf http://localhost:4170/api/v1/health > /dev/null 2>&1; then
              echo "Backend healthy after ${i}s"
              break
            fi
            sleep 1
          done
          # Wait for frontend to be serving
          echo "Waiting for frontend..."
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3170/ > /dev/null 2>&1; then
              echo "Frontend healthy after ${i}s"
              break
            fi
            sleep 1
          done
          SCRIPT

  e2e-staging:
    name: E2E Tests (Staging)
    runs-on: ubuntu-latest
    needs: deploy-staging
    # E2E IS a release gate. `deploy-production` needs this job, and with
    # continue-on-error the gate was decoration: the job went red, GitHub
    # counted it as satisfied for its dependents, and prod shipped anyway on
    # failing browser tests.
    #
    # It was switched off on the belief that "UI-assertion fixtures have been
    # drifting from the actual UI faster than they're being repaired". That
    # diagnosis was wrong. Every failure was ERR_CERT_COMMON_NAME_INVALID: the
    # STAGING_HOST secret held an IP instead of the hostname, so the tests hit
    # the box by address, nginx (which routes ~10 staging vhosts by Host
    # header) 301'd to HTTPS, and no certificate can match a bare IP. The tests
    # could not reach the site at all. Pointing the secret back at
    # staging-<brand>.forjio.com turned all six failing suites green with no
    # test changes (2026-07-13).
    #
    # A broken environment was misfiled as flaky tests and the response was to
    # stop listening. If this job goes red, it means something is broken.
    steps:
      - uses: actions/checkout@v5
      # Staging runs on the SHARED box reached only over the tailnet
      # (taila71a7b.ts.net). Join as an ephemeral CI node, then map the
      # staging hostname → the box's tailnet IP in the runner's /etc/hosts.
      # We KEEP the hostname (not the raw IP) because nginx routes the ~10
      # products' staging vhosts by Host header — so e2e must send
      # Host: staging-<brand>.forjio.com while the connection rides the
      # tailnet. E2E_BYPASS_SECRET still gates auth.
      - name: Connect to tailnet (ephemeral CI node)
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TS_AUTHKEY }}
          hostname: suppuo-e2e-ci
      - name: Route staging host over the tailnet
        run: echo "100.66.19.22 ${{ secrets.STAGING_HOST }}" | sudo tee -a /etc/hosts
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Install Playwright
        run: cd e2e && npm ci && npx playwright install chromium --with-deps
      - name: Wait for staging (backend + frontend)
        run: |
          echo "Waiting for backend..."
          for i in $(seq 1 30); do
            if curl -sf http://${{ secrets.STAGING_HOST }}/api/v1/health > /dev/null 2>&1; then
              echo "Backend healthy"
              break
            fi
            echo "  attempt $i..."
            sleep 3
          done
          echo "Waiting for frontend..."
          for i in $(seq 1 20); do
            STATUS=$(curl -so /dev/null -w '%{http_code}' http://${{ secrets.STAGING_HOST }}/ 2>/dev/null || echo "000")
            if [ "$STATUS" = "200" ]; then
              echo "Frontend serving (HTTP 200)"
              break
            fi
            echo "  attempt $i (HTTP $STATUS)..."
            sleep 3
          done
          # Extra settle time for server stability
          sleep 5
      - name: Run E2E
        env:
          # The authenticated gate (tests/dashboard.spec.ts) signs in for real
          # as gojo, the designated E2E identity. It does NOT self-register:
          # this staging product authenticates against PRODUCTION Huudis, so a
          # signup here would mint a real user in the production identity DB on
          # every push. The spec THROWS if this secret is unset; it does not
          # skip. A gate that quietly skips on a missing secret is the inert
          # gate we replaced.
          GOJO_HUUDIS_EMAIL: gojo@forjio.com
          GOJO_HUUDIS_PASSWORD: ${{ secrets.GOJO_HUUDIS_PASSWORD }}
        run: |
          cd e2e
          BACKEND_URL=http://${{ secrets.STAGING_HOST }}/api/v1 \
          FRONTEND_URL=http://${{ secrets.STAGING_HOST }} \
          E2E_BYPASS_SECRET=${{ secrets.E2E_BYPASS_SECRET }} \
          PW_SKIP_WEBSERVER=1 \
          npx playwright test --reporter=list --config=playwright.ci.config.ts

      # Upload screenshots + traces on failure so we can actually
      # diagnose what the test saw. Without this, the only signal is
      # "locator not found" — no visibility into whether the page
      # 404'd, redirected to /login, or rendered empty.
      - name: Upload Playwright artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-results
          path: |
            e2e/test-results/
            e2e/playwright-report/
          retention-days: 7
          if-no-files-found: ignore

  visual-regression:
    name: Visual Regression (Staging)
    # Drift detection is opt-in: dispatch with update_baselines=true to
    # capture fresh baselines, or without the input to verify no drift.
    # Push events never run this job so content commits land green even
    # before the baseline-refresh dispatch.
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    needs: deploy-staging
    # Gates production on chrome stability. 0.5% pixel diff per page
    # against committed baselines at
    # e2e/tests/visual.spec.ts-snapshots/. Baselines are CI-generated
    # (workflow_dispatch with update_baselines=true) — never regenerate
    # locally; font/raster mismatch between dev-machine and the GHA
    # runner produces noisy false positives.
    permissions:
      contents: write
    steps:
      # Staging is tailnet-only — join + map the staging host(s) to the
      # box's tailnet IP before any step contacts staging.
      - name: Connect to tailnet (ephemeral CI node)
        uses: tailscale/github-action@v2
        with:
          authkey: ${{ secrets.TS_AUTHKEY }}
          hostname: suppuo-visual-ci
      - name: Route staging host over the tailnet
        run: |
          echo "100.66.19.22 ${{ secrets.STAGING_HOST }}" | sudo tee -a /etc/hosts
          echo "100.66.19.22 staging-suppuo.forjio.com" | sudo tee -a /etc/hosts
      - uses: actions/checkout@v5
        with:
          # Need a writable checkout for the baseline-update branch.
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Install Playwright
        run: cd e2e && npm ci && npx playwright install chromium --with-deps
      - name: Wait for staging frontend
        run: |
          echo "Waiting for staging frontend..."
          for i in $(seq 1 30); do
            STATUS=$(curl -so /dev/null -w '%{http_code}' http://${{ secrets.STAGING_HOST }}/ 2>/dev/null || echo "000")
            if [ "$STATUS" = "200" ]; then
              echo "Frontend serving (HTTP 200) after ${i}s"
              break
            fi
            echo "  attempt $i (HTTP $STATUS)..."
            sleep 3
          done
          sleep 5
      - name: Run visual regression
        id: visual
        run: |
          cd e2e
          FRONTEND_URL=http://${{ secrets.STAGING_HOST }} \
            npx playwright test \
              --config=playwright.visual.config.ts \
              ${{ (github.event_name == 'workflow_dispatch' && inputs.update_baselines) && '--update-snapshots' || '' }}
      - name: Commit refreshed baselines
        if: github.event_name == 'workflow_dispatch' && inputs.update_baselines
        run: |
          cd e2e
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          # Only stage the snapshot dir — never accidentally commit
          # test-results/ or playwright-report/.
          git add 'tests/visual.spec.ts-snapshots' || true
          if git diff --cached --quiet; then
            echo "No baseline changes to commit"
          else
            git commit -m "chore(visual): refresh marketing baselines via CI"
            git push origin HEAD:${{ github.ref_name }}
          fi
      - name: Upload visual diffs on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: visual-diffs
          path: |
            e2e/test-results/
            e2e/playwright-report/
          retention-days: 14
          if-no-files-found: ignore

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    # Gates on e2e-staging + visual-regression. Previous setting
    # decoupled e2e from prod ("E2E is advisory") was reverted
    # 2026-05-09 per bang's "all CI = huudis" directive.
    needs: [e2e-staging]
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && github.repository != 'hachimi-cat/forjio-service-template'
    steps:
      # Reuses the prod artifact from the Build job — no re-checkout, no
      # rebuild. The production frontend build inlines
      # NEXT_PUBLIC_TURNSTILE_SITE_KEY (the staging build omits it), so prod
      # gets its own artifact. Backend dist is env-independent and shared.
      - uses: actions/download-artifact@v7
        with:
          name: backend-dist
          path: backend/
      - uses: actions/download-artifact@v7
        with:
          name: frontend-out-production
          path: frontend/
      - name: Deploy via rsync
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avz --delete --exclude node_modules --exclude .git --exclude .env --exclude .npmrc --exclude frontend/.next/cache
          path: ./
          remote_path: /opt/saas/suppuo/
          remote_host: ${{ secrets.PRODUCTION_HOST }}
          remote_user: root
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
      - name: Migrate + restart
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.PRODUCTION_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 30m
          script_stop: true
          script: |
            cd /opt/saas/suppuo/backend
            sed -i '/^TURNSTILE_SECRET_KEY=/d' .env 2>/dev/null || true
            echo 'TURNSTILE_SECRET_KEY=${{ secrets.TURNSTILE_SECRET_KEY }}' >> .env
            sed -i '/^TURNSTILE_ALLOWED_HOSTNAMES=/d' .env 2>/dev/null || true
            echo 'TURNSTILE_ALLOWED_HOSTNAMES=${{ secrets.TURNSTILE_ALLOWED_HOSTNAMES }}' >> .env
            npm ci --omit=dev
            npx prisma generate
            npx prisma migrate deploy
            pm2 restart suppuo || pm2 start dist/index.js --name suppuo -i 1
            cd /opt/saas/suppuo/frontend
            # Dynamic Next app — `.next/` pre-built in the artifact.
            npm ci --omit=dev
            pm2 restart suppuo-frontend || pm2 start npm --name suppuo-frontend -- start
            pm2 save

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: deploy-production
    if: startsWith(github.event.head_commit.message, 'v')
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Extract version
        id: ver
        run: |
          VER=$(echo "${{ github.event.head_commit.message }}" | grep -oP '^v\d+\.\d+\.\d+' || echo "")
          echo "version=$VER" >> $GITHUB_OUTPUT
      - name: Create GitHub Release
        if: steps.ver.outputs.version
        run: |
          gh release create "${{ steps.ver.outputs.version }}" \
            --title "${{ steps.ver.outputs.version }}" \
            --generate-notes \
            --target ${{ github.sha }}
        env:
          GH_TOKEN: ${{ github.token }}
      - name: Publish CLI if changed
        if: steps.ver.outputs.version
        run: |
          if git diff --name-only HEAD~1 2>/dev/null | grep -q '^cli/'; then
            cd cli
            npm version patch --no-git-tag-version 2>/dev/null || true
            npm publish --access public 2>/dev/null || echo "CLI publish skipped"
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
